#include "cache.h"
#include <bits/stdc++.h>
using namespace std;




// initialize replacement state
void CACHE::llc_initialize_replacement()
{
  
}

// find replacement victim
uint32_t CACHE::llc_find_victim(uint32_t cpu, uint64_t instr_id, uint32_t set, const BLOCK *current_set, uint64_t ip, uint64_t full_addr, uint32_t type)
{
    random_device rd;  // A random seed source

    mt19937 generator(rd());  // Mersenne Twister PRNG

    uniform_int_distribution<int> distribution(0, LLC_WAY-1);  // Uniform distribution between 0 and 15 inclusive
  
    // Generate a random number
    uint32_t way = distribution(generator);
    if(way<LLC_WAY)
        return way;     //random way will be returned by the function.

    // WE SHOULD NOT REACH HERE
    assert(0);
    return 0;
}

// called on every cache hit and cache fill
void CACHE::llc_update_replacement_state(uint32_t cpu, uint32_t set, uint32_t way, uint64_t full_addr, uint64_t ip, uint64_t victim_addr, uint32_t type, uint8_t hit)
{
    //in random replacement policy there is no need to update the replacement state.
    
}

// use this function to print out your own stats at the end of simulation
void CACHE::llc_replacement_final_stats()
{

}
